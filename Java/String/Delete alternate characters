class Solution {
    static String delAlternate(String S) {
        // code here
        String ans="";
        for (int i=0;i<S.length();i++)
        {
            if(i%2!=0)
            {
                continue;
            }
            ans=ans+S.charAt(i);
        }
        return ans;
    }
}
